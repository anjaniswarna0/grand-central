# Changelog

All notable changes to these reusable workflows are tracked here so consumers know when it's safe to bump their pinned version.

## v1.0.0 - Unreleased

- Initial reusable-build-and-test.yml workflow
- Added actionlint check on PRs touching .github/workflows/**
